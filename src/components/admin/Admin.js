import React from 'react';
import Layout from '../Layout';
import Login from './Login';
const Admin = () => {
  return (
    <Layout>
      <Login />
      <div>Admin</div>
    </Layout>
  );
};

export default Admin;
